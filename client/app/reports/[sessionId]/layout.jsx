/**
 * Server segment config for static export (`output: "export"`).
 */
export function generateStaticParams() {
  return [];
}

export default function ReportsSessionLayout({ children }) {
  return children;
}
